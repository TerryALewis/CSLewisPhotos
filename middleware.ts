// Middleware intentionally disabled.
// All auth checks run client-side in plugins (plugins/auth.client.ts).
// Keeping this file as a placeholder prevents accidental server-side imports.

// No exported middleware.
